<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :cod %><br>
    <%= f.text_field :cod %>
  </div>
 
  <!-- Esto lo vamos a sacar del user que este dando el alta de la categoria en el sistema  
  <div class="field">
    <%= f.label :sector_id %><br>
    <%= f.select :sector_id , options_for_select(Sector.all.map { |s| [s.nombre, s.id] })%>
  </div>
 -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
